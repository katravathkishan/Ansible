- name: install packages
  hosts: local
  connection: local
  tasks:
  - name: install packages
    ansible.builtin.package:
      msg: " hello {{item}}"
    loop:
    - nginx
    - mysql
    - git
    - zip